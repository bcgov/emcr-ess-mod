name: CI Registrants UI

on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - edited
      - reopened
      - synchronize
      - ready_for_review
    branches:
      - master
    paths:
      - "registrants/src/UI/embc-registrant/**"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22.x"
          cache: "npm" # handles caching, no need to have seperate actions/cache@v4, caches what npm install downloads.
          cache-dependency-path: registrants/src/UI/embc-registrant/package-lock.json

      - name: Install Dependencies
        run: |
          cd registrants/src/UI/embc-registrant
          npm install --ignore-scripts
          if [ $? -ne 0 ]; then
            echo "::error::Failed to install npm packages"
            exit 1
          fi

      - name: Build
        run: |
          cd registrants/src/UI/embc-registrant
          npm run build -- --configuration production
          if [ $? -ne 0 ]; then
            echo "::error::Build failed - check the logs above for compilation errors"
            exit 1
          fi
