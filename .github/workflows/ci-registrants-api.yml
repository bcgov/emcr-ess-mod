name: CI Registrants API

on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - edited
      - reopened
      - synchronize
      - ready_for_review
    branches:
      - master
    paths:
      - "registrants/src/API/**"
      - "shared/src/**"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('registrants/src/API/**/*.csproj', 'shared/src/**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "8.0.x" # Install .NET 8.0

      - name: Restore dependencies
        run: |
          dotnet restore registrants/src/API/EMBC.Registrants.API/EMBC.Registrants.API.csproj --disable-parallel  # Download all NuGet packages
          if [ $? -ne 0 ]; then
            echo "::error::Failed to restore NuGet packages"
            exit 1
          fi

      - name: Build
        run: |
          dotnet build registrants/src/API/EMBC.Registrants.API/EMBC.Registrants.API.csproj --no-restore
          if [ $? -ne 0 ]; then
            echo "::error::Build failed - check the logs above for compilation errors"
            exit 1
          fi
